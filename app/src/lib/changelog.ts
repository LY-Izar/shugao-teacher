/**
 * 更新日志（「我的 → 更新日志」那一屏的**唯一来源**）。
 *
 * ## 为什么从 `pages/Settings.tsx` 挪到这里
 *
 * 它原来是硬编码在设置页里的一个数组，而**发版三步的第③步就是"在它顶部加一段"**——
 * 版本号（`lib/version.ts` 的 `APP_VERSION`）和它分居两个文件，于是真的走散了：
 * 设置页「关于」里写着 v0.9.0，同一页下面的更新日志却停在 0.8.0，自己跟自己打架。
 * 现在发版 = **一个文件两处改**（`APP_VERSION` + 这里顶部加一段），第②步是 `app/package.json`。
 *
 * ## 写法上的纪律（写给老师看的，不是 changelog 生成器）
 *
 * - **只写教师能感觉到的那件事**：不写文件名、不写"重构""收口"这类内部词、
 *   不写内部编号（W16 / I8 这种只在 `功能设计与不变量.md` 里有意义）。
 * - 🔴 **只写"更新了什么功能"**（用户 2026-09-29 拍板）。以下这些**一律不写**：
 *   超管运维面板、账号隔离、拍照识别额度、移动端导航换色、
 *   自动化检查那一类**只对维护者有意义**的条目。
 *   它们不是被删掉了 —— 它们记在 `功能设计与不变量.md` 里（那份是给维护者看的），
 *   而这一屏的读者是**老师**。判据：老师读完之后能不能感觉到平台变了。
 * - 🔴 **正文里不许留 Markdown 星号**（`**…**`）：这一屏是纯文本渲染，
 *   星号会原样显示成字面星号。要强调就用短句本身。
 * - **最新的在最上面**，第一条的 `v` 必须与 `APP_VERSION` 对得上。
 * - `at` 是**发版日**（`9月25日` 这种写法：月日之间**不空格**，全表统一）。
 *   ⚠️ **没有确切的发版日就不要编**：写 `待定`，等真发版那天再填。
 *   编一个日期比空着更糟——这一屏是给老师看的时间线，而"哪天改的"是它唯一的信息量之一。
 * - 🔴 **`at` 的口径（2026-09-25 定，纠了一处真实的矛盾）**：
 *   它 = **这套改动真的落进仓库的那一天**（= 那批提交的 `git log` 日期）。
 *   **不是**"计划发布日"、**不是**"凑整的漂亮数字"、**不是**"下一次发版日"。
 *   反例（已修）：`0.8.0` 原来写 `9 月 26 日`，而它的 15 条改动全部落在
 *   `2026-09-21` / `09-22` 的提交里（版本号本身是 `62fa8da`（2026-09-22）提的），
 *   于是它比后一版 `0.9.0`（`9月25日`）还晚 —— **版本倒挂**，老师看到的时间线是反的。
 *   现在 0.8.0 = `9月22日`（有据可依：那批提交的最后一天）。
 *   规矩本身写在 `功能设计与不变量.md` §一 · 1.1「日期口径」。
 * - 版本之间**不重复**：已经写进 0.8.0 的事，不要在 0.9.0 里再说一遍。
 */

export type ChangeLogEntry = {
  /** 版本号（与 `lib/version.ts` 的 `APP_VERSION` 一致） */
  v: string
  /** 发版日，例：`9 月 26 日`；还没定就写 `待定`（别编） */
  at: string
  items: string[]
}

export const CHANGELOG: ChangeLogEntry[] = [
  {
    v: '0.9.1',
    // 发版日由用户给定（2026-09-25）
    at: '9月25日',
    items: [
      '学号：每个学生多了一个入学年份 + 序号组成的固定编号（如 2025001）；班上 1、2、3 号的学号照旧可改',
      '身份更细：新增校长、副校长、校长助理、办公室主任、德育处主任、教研组长、备课组长。谁是哪个身份，登录后能看到的页面就跟着不同',
      '通知：可以按部门发（办公室 / 教务处 / 总务处 / 德育处），也可以按职位发；教务处主任现在也在「按职位发」的名单里',
      '公告：最高管理员可以发布全站公告，分普通、重要、紧急三档，能置顶、能设起止时间。重要和紧急的会在打开平台时弹一次',
      '教师账号页可以给老师标部门（一个人可以属于多个部门），也支持一次给一批人标',
      '数据库备份改成加密保存，下载后要口令才能打开',
    ],
  },
  {
    v: '0.9.0',
    // 发版日由用户给定（2026-09-25）
    at: '9月25日',
    items: [
      '全学科通用：不再只服务物理 —— 作业档案带学科，列表可按学科分类看（一科一段，写清这一科几份待收缴、待批改），练习册模板按学科过滤，呼叫地点默认按学科算',
      '九科知识体系：高一到高三九个学科的章节与知识点（1600 多个），错题按知识点归因，能看出"哪个板块没掌握"而不是只知道错了第几题',
      '考试：可以新建档案、导入成绩单或逐题录分，然后看逐题得分率与年级排名',
      '教师账号：建号时可以带上学科；学科老师只看得到自己教的学科，班主任与年级主任能看见一个班的所有学科',
      '成绩权限：科任老师只能改自己任教班级、自己那一科；班主任与年级主任看得到、改不了',
      '错题集改成两层：先选班，再进这个班的错题档案（学生名单 + 右上角「班级总结错题」与重练卷），不再先切"当前班级"',
      '移动端导航改版：底部换成悬浮玻璃胶囊（工作台 / 作业 / 我的）+ 右侧展开按钮（班级 / 考试 / 错题集 / 日程表），胶囊可以按住拖动',
      '教室端：多科老师同时呼叫会依次播报，不再互相顶掉；断线后会自动补收漏掉的呼叫；「正在上课」卡显示科目与任课老师；按日期选作业改成点一下展开',
      '教室端：节假日不显示课表；课表拍不清楚时可以粘贴补录，导进来先逐条核对再入库；下课前 5 分钟轻声提示、周三下午与考试期间自动静音（呼叫排队等考完）',
      '登录页与设置页显示 `版本号 · 构建哈希` —— 线上跑的是哪一次构建一眼可查',
      '恢复备份：修掉"恢复之后学科丢失、下一次批改把云端学科写成空"这个不可逆的问题；备份里的题量越界也会被收回合法区间',
      '题图超预算时所有图一起降档（不再砍后面的题），降了几张会明说；一张扫描图不再拖垮整份档案的保存',
    ],
  },
  {
    v: '0.8.0',
    // 2026-09-25 修正：原来写 `9 月 26 日` —— 比后一版（0.9.0 = 9月25日）还晚，是版本倒挂。
    // 这 15 条改动全部落在 2026-09-21 / 09-22 的提交里，版本号本身是 62fa8da（2026-09-22）提的，
    // 所以按"改动真的落进仓库的那一天"写 9月22日（口径见本文件顶部与功能设计与不变量 §一 · 1.1）。
    at: '9月22日',
    items: [
      '作业档案可同时布置给多个班（每个班各生成一份，批改统计各算各的）',
      '完成批改分成两条路：临时保存（可随时接着批）／确认完成（未批改的登记为未交）',
      '新增「改错登记」：逐个点过关，谁改完了、谁还没改一目了然',
      '需重点关注名单：批改时点「找」标一下，改错登记里置顶提醒',
      '新增「极简模式」：不带练习册也能用，只记每人 优 / 良 / 差',
      '题号上不再有会误触的「+」：双击拆小题、长按改小题数',
      '布置日期建完之后也能改',
      '教室端：考试一键静音（全屏黑底时钟）、下课前 5 分钟轻声提示',
      '教室端：调休日可手动选「今天按周几的课表上」，周一早三节自动顺延',
      '教室端：晚上 7:20 后换成收尾语，0 点自动恢复；节日和周末倒计时',
      '教室端：拍照识别课表后可逐条核对修改时间再导入',
      '教师端可把题图 / PDF / HTML / PPT 传到教室一体机上打开，文件不占云端',
      '错题集：按知识点统计丢分、生成错题重练卷（Word）、一键备份与恢复',
      '批改中途切出去不会丢进度，回来接着批',
      '所有名单统一按学号排序；呼叫记录只留最近 3 条',
    ],
  },
  {
    v: '0.7.0',
    at: '9月19日',
    items: [
      '云端同步：手机与教室端共用一份数据，呼叫真的能跨设备送达',
      'Word 稿一键导入建立作业档案（题量 / 题型 / 分值 / 小问自动识别，可逐题改）',
      '作业情况新增「题型掌握情况」：每个题型丢几分、正确率多少',
      '课表支持批量录入与课表文件导入，上课前 10 分钟提醒',
      '拍照查缺改为真识别：先数本数，够数直接判全过',
      '法定假期与调休按官方安排自动判定（含调休上班日）',
    ],
  },
  {
    v: '0.5.0',
    at: '9月18日',
    items: [
      '逐题错误率与讲评优先级（30–70% 优先精讲）',
      '改错一键呼叫：教室端全屏播报 + 置顶小窗',
      '教室端：系统语音播报、逐题正确率、压在全屏应用之上的小窗',
      '快速批改录入：点学号就地展开题号，默认全对只记错的',
    ],
  },
  {
    v: '0.2.0',
    at: '9月17日',
    items: [
      '班级与花名册：拍照/粘贴导入、序列自检、名单体检',
      '作业档案与收作业查缺、未交与迟交分开记录',
    ],
  },
]
