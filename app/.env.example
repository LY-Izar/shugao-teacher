# Supabase 连接配置
#
# 用法：复制本文件为 .env.local，填入真实值（.env.local 已被 .gitignore 排除）
#
#   VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
#   VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxx
#
# ⚠️ 这两个值会打进前端产物，是**公开**的（publishable / anon key）。
#    安全完全靠数据库的 RLS 策略，不靠把 key 藏起来。
#
# ⚠️ 绝对不要把 service_role / sb_secret_ 开头的密钥填在这里 ——
#    那个能绕过所有权限，泄露等于数据库全开。它只应该待在服务端。
#
# 生产环境：在 Cloudflare Pages → Settings → Environment variables 里配同名的两个变量，
#          然后对最近一次部署点 Retry deployment（改环境变量需要重新构建才生效）。
#
# 两个变量都不填时，应用自动退回浏览器本地存储模式（localStorage），功能照常可用。

# ⚠️ 服务端（Cloudflare Pages Functions）另有几个环境变量 —— `SUPABASE_SERVICE_ROLE_KEY` /
#    `RESEND_API_KEY` / `ADMIN_NOTIFY_EMAIL`（**邮件收件人，由部署环境决定**）/ `R2_*` /
#    `GITHUB_*`。它们**不是** `VITE_` 变量、**不属于这个文件**（写在这里也不会被打进产物）：
#    清单与"没配会怎样"见 `README.md` 的「云端环境变量」那一节。

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
